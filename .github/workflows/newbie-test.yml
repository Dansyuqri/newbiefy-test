name: 'newbiefy-test'
on:
  create:
    branches:
      - newbiefy*

jobs:
  newbiefy:
    runs-on: ubuntu-latest
    steps:
      - uses: dansyuqri/newbiefy@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}